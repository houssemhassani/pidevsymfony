{% extends 'base.html.twig' %}

{% block title %}Gestion Citoyen{% endblock %}

{% block body %}
    <main id="main" class="main">

        <section class="section">
            <div class="row">
                <div class="col-lg-12">

                    <div class="card">
                        <div class="card-body">
                            <a href="{{ path('app_gestion_employee_new') }}"><i class="ri-arrow-left-circle-fill" style="font-size: 50px;"></i></a>
                            <h5 class="card-title">Liste des Citoyens</h5>
                            <!-- Table with stripped rows -->
                            <table class="table datatable">
                                <thead>
                                <tr>
                                    <th scope="col">Nom</th>
                                    <th scope="col">Prénom</th>
                                    <th scope="col">CIN</th>
                                    <th scope="col">Email</th>
                                    <th scope="col">actions</th>
                                </tr>
                                </thead>
                                <tbody>
                                {% for employee in citoyens %}
                                    <tr>
                                        <td>{{ employee.nom }}</td>
                                        <td>{{ employee.prenom }}</td>
                                        <td>{{ employee.email }}</td>
                                        <td>{{ employee.cin }}</td>
                                        <td>
                                            <a href="{{ path('app_gestio_citoyen_show', {'id': employee.id}) }}">Détail</a>
                                            <a href="{{ path('app_gestion_citoyen_refuser', {'id': employee.id}) }}">Refuser</a>
                                            <a href="{{ path('app_gestion_citoyen_accepter',{'id':employee.id}) }}">Accepter</a>
                                        </td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                            <br/>
                            <a href="{{ path('app_gestio_citoyen_new') }}">Ajouter nouveau</a>
                            <!-- End Table with stripped rows -->

                        </div>
                    </div>

                </div>
            </div>
        </section>

    </main><!-- End #main -->


{% endblock %}
