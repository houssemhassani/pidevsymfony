{% extends 'base.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <h1>Admin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admin.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ admin.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ admin.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ admin.email }}</td>
            </tr>
            <tr>
                <th>Cin</th>
                <td>{{ admin.cin }}</td>
            </tr>
            <tr>
                <th>MotDePasse</th>
                <td>{{ admin.motDePasse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gestion_admin_index') }}">back to list</a>

    <a href="{{ path('app_gestion_admin_edit', {'id': admin.id}) }}">edit</a>

    {{ include('gestion_admin/_delete_form.html.twig') }}
{% endblock %}
