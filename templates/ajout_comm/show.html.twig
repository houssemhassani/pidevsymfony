{% extends 'baseadmin.html.twig' %}



{% block body %}
    <h1>Commentaire</h1>

    <div class="col-lg-12" style="display: flex;align-items: center;justify-content: center;margin-top: 150px;padding-left: 250px">

        <div class="card">
            <div class="card-body">
                <a  href="{{ path('app_ajout_comm_index', {'id': commantaire.id}) }}"><i class="ri-arrow-left-circle-fill" style="font-size: 30px; padding-left: 105px"></i></a> <center> <h5 class="card-title">Affichage du Commentaire</h5> </center>


                <!-- Table with stripped rows -->
                <div class="dataTable-wrapper dataTable-loading no-footer sortable searchable fixed-columns"></div></div><div class="dataTable-container"><table class="table datatable dataTable-table">
                    <thead>


                    </thead>
                    <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ commantaire.id }}</td>
                    </tr>
                    <tr>
                        <th>Content</th>
                        <td>{{ commantaire.content }}</td>
                    </tr>



                    </tbody>

                </table>
            </div>
            <form method="post"  action="{{ path('app_ajout_comm_delete', {'id': commantaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commantaire.id) }}">
                <center><button class="btn btn-primary" >Delete</button> </center>
            </form>

            <nav class="dataTable-pagination"><ul class="dataTable-pagination-list"></ul></nav></div></div>
    <!-- End Table with stripped rows -->

    </div>

    </div>

    </div>

    <table class="table">

    </table>

    {{ include('ajout_comm/_delete_form.html.twig') }}




{% endblock %}
