{% extends 'base.html.twig' %}

{% block title %}Notification index{% endblock %}

{% block body %}
<div class="row" style="display: flex;align-items: center;justify-content: center;margin-top:150px;">
    <div class="col-lg-7">

        <div class="card">
            <div class="card-body">

                <a href="{{ path('app_notification_new') }}"><i class="ri-arrow-left-circle-fill" style="font-size:50px;"></i></a>
                <h5 class="card-title">Notification</h5>

                <!-- Table with stripped rows -->
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>TypeNotification</th>
                <th>EmailNotification</th>
                <th>NumNotification</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for notification in notifications %}
            <tr>
                <td>{{ notification.id }}</td>
                <td>{{ notification.typeNotification }}</td>
                <td>{{ notification.emailNotification }}</td>
                <td>{{ notification.numNotification }}</td>
                <td>
                    <a class="btn btn-success" href="{{ path('app_notification_show', {'id': notification.id}) }}">delete</a>
                    <a class="btn btn-danger"  href="{{ path('app_notification_edit', {'id': notification.id}) }}">edit</a>
                </td>
            </tr>

        {% endfor %}
        </tbody>
    </table>
                <!-- End small tables -->

            </div>
        </div>

    </div>

</div>

{% endblock %}
