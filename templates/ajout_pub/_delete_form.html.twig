{% extends 'baseadmin.html.twig' %}
{% block body %}
    <form method="post"  action="{{ path('app_ajout_pub_delete', {'id': publication.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ publication.id) }}">
        <button class="bi bi-trash-fill" >Delete</button>
    </form>
{% endblock %}