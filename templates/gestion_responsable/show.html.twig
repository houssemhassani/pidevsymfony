{% extends 'base.html.twig' %}

{% block title %}Employee{% endblock %}

{% block body %}
    <h1>Employee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employee.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ employee.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ employee.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ employee.email }}</td>
            </tr>
            <tr>
                <th>Cin</th>
                <td>{{ employee.cin }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ employee.photo }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ employee.role }}</td>
            </tr>
            <tr>
                <th>MotDePasse</th>
                <td>{{ employee.motDePasse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gestion_responsable_index') }}">back to list</a>

    <a href="{{ path('app_gestion_responsable_edit', {'id': employee.id}) }}">edit</a>

    {{ include('gestion_responsable/_delete_form.html.twig') }}
{% endblock %}
